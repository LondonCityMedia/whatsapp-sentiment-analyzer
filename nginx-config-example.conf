# Example nginx configuration for api.analyser.chat
# If you have nginx in front of your backend, add this to your nginx config

server {
    listen 80;
    server_name api.analyser.chat;

    # Increase client body size limit (for file uploads)
    client_max_body_size 20M;
    client_body_buffer_size 20M;

    # Increase timeouts for large file uploads
    proxy_connect_timeout 600;
    proxy_send_timeout 600;
    proxy_read_timeout 600;
    send_timeout 600;

    location / {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Increase buffer sizes
        proxy_buffering off;
        proxy_request_buffering off;
    }
}

# If using SSL (recommended)
server {
    listen 443 ssl http2;
    server_name api.analyser.chat;

    # SSL certificates (adjust paths as needed)
    # ssl_certificate /path/to/cert.pem;
    # ssl_certificate_key /path/to/key.pem;

    # Increase client body size limit (for file uploads)
    client_max_body_size 20M;
    client_body_buffer_size 20M;

    # Increase timeouts for large file uploads
    proxy_connect_timeout 600;
    proxy_send_timeout 600;
    proxy_read_timeout 600;
    send_timeout 600;

    location / {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Increase buffer sizes
        proxy_buffering off;
        proxy_request_buffering off;
    }
}


